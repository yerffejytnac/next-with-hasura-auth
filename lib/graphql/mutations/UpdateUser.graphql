mutation UpdateUser($id: String!, $name: String, $image: String) {
  update_users_by_pk(
    pk_columns: { id: $id }
    _set: { name: $name, image: $image }
  ) {
    id
    email
    image
    name
  }
}
